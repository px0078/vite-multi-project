{
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "yarn build:editor & yarn build:site",
    "serve": "yarn serve:editor & yarn serve:site",
    "serve:editor": "node scripts/serve --project editor",
    "build:editor": "node scripts/build --project editor",
    "serve:site": "node scripts/serve --project site",
    "build:site": "node scripts/build --project site"
  },
  "dependencies": {
    "vue": "^3.0.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^1.2.3",
    "@vue/compiler-sfc": "^3.0.5",
    "cac": "^6.7.3",
    "typescript": "^4.1.3",
    "vite": "^2.3.5",
    "vue-tsc": "^0.0.24"
  }
}
